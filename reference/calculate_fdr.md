# Calculate False Discovery Rate

Estimates the false discovery rate for a set of tests

## Usage

``` r
calculate_fdr(p_values, alpha = 0.05)
```

## Arguments

- p_values:

  Numeric vector of p-values

- alpha:

  FDR level (default 0.05)

## Value

List with FDR estimates
