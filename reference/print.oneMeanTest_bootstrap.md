# Print method for bootstrap one-sample t-test

Pretty printing for objects of class `"oneMeanTest_bootstrap"` produced
by [`bootstrap_ttest`](bootstrap_ttest.md).

## Usage

``` r
# S3 method for class 'oneMeanTest_bootstrap'
print(x, digits = 4, ...)
```

## Arguments

- x:

  An object of class `"oneMeanTest_bootstrap"`.

- digits:

  Number of digits to use for printing numeric values.

- ...:

  Unused; included for method compatibility.

## Value

Invisibly returns `x`.
